query GetSnippet($id: String!) {
  snippet(id: $id) {
    code
    highlightedCodeHtml
    id
    language
    postedAt
    title
    userId
    user {
      displayName
      id
      imageUrl
      username
    }
  }
}
